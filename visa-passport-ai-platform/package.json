{
  "name": "visa-passport-ai-platform",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered visa and passport processing marketplace monorepo",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "corepack pnpm -r --if-present build",
    "build:packages": "corepack pnpm --filter './packages/*' --if-present build",
    "dev": "corepack pnpm build:packages && concurrently --kill-others-on-fail --names web,worker,ocr --prefix-colors cyan,magenta,yellow \"corepack pnpm dev:web\" \"corepack pnpm dev:worker\" \"corepack pnpm dev:ocr\"",
    "dev:web": "dotenv -e .env -- corepack pnpm --filter @visa-platform/web dev",
    "dev:worker": "dotenv -e .env -- corepack pnpm --filter @visa-platform/worker dev",
    "dev:ocr": "dotenv -e .env -- python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8001 --app-dir apps/ocr-service",
    "dev:infra": "docker compose -f infra/docker-compose.yml up -d postgres redis",
    "dev:setup": "pnpm db:generate && pnpm db:migrate",
    "dev:all": "pnpm dev:infra && pnpm dev:setup && pnpm dev",
    "lint": "corepack pnpm -r --if-present lint",
    "typecheck": "corepack pnpm -r --if-present typecheck",
    "test": "corepack pnpm -r --if-present test && corepack pnpm test:ocr",
    "test:ocr": "cd apps/ocr-service && python -m pytest",
    "db:generate": "dotenv -e .env -- corepack pnpm --filter @visa-platform/database db:generate",
    "db:migrate": "dotenv -e .env -- corepack pnpm --filter @visa-platform/database db:migrate",
    "db:migrate:dev": "dotenv -e .env -- corepack pnpm --filter @visa-platform/database db:migrate:dev",
    "db:studio": "dotenv -e .env -- corepack pnpm --filter @visa-platform/database db:studio",
    "docker:up": "docker compose -f infra/docker-compose.yml up --build",
    "docker:down": "docker compose -f infra/docker-compose.yml down",
    "infra:up": "docker compose -f infra/docker-compose.yml up --build -d",
    "infra:down": "docker compose -f infra/docker-compose.yml down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^8.0.0",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "sharp"
    ]
  }
}
